# --- web / api ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9        # file uploads
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
httpx>=0.27

# --- agent / rag ---
langgraph>=0.0.40
langchain>=0.1.16
langchain-community>=0.0.34
rank-bm25>=0.2.2               # hybrid retrieval (sparse)
sentence-transformers>=2.7     # embeddings + cross-encoder re-ranker
langdetect>=1.0.9             # language detection

# --- llm providers (use whichever LLM_PROVIDER you set) ---
openai>=1.23
anthropic>=0.25
# ollama is called over HTTP via httpx; no pip package required

# --- database / vectors ---
psycopg[binary]>=3.1
pgvector>=0.2.5
SQLAlchemy>=2.0

# --- document parsing (multi-format) ---
pymupdf>=1.24                  # digital PDF
pytesseract>=0.3.10           # OCR (needs system tesseract + tesseract-ocr-deu)
pdf2image>=1.17               # render PDF pages for OCR (needs poppler)
Pillow>=10.3
python-docx>=1.1              # .docx
openpyxl>=3.1                 # .xlsx
pandas>=2.2                   # tables / .csv
python-pptx>=0.6.23           # .pptx
beautifulsoup4>=4.12          # .html
# legacy .doc handled via system LibreOffice/antiword (see README), not pip

# --- dev ---
pytest>=8.1
